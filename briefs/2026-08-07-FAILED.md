☀️ *Morning Brief* — Friday, Aug 7

⚠️ *Sources degraded:* reddit — 403 (blocked). Brief is HN + GitHub only.

The industry just standardized the *plumbing* for agent add-ons — and deliberately left the *safety* out. Vercel-initiated *Agent Plugins* (steering: Amazon, Cursor, Microsoft, OpenAI, Vercel) bundles MCP servers + Agent Skills into one `plugin.json` folder, "build once, run anywhere" across ChatGPT, Copilot, Cursor, Kiro, VS Code. But the spec is "deliberately small": marketplaces, installation, *permissions, sandboxing, and trust* all stay with each client. Same week, 40k plays of a human-in-the-loop game show that's exactly the layer nobody can hold: mean accuracy 66%, and exfiltration commands missed *one-third of the time*. The safety layer everyone's standardizing around is the one nobody owns. That gap is your lane.

🔥 *Hot Signals*
- *Agent Plugins 1.0* published — one package format across five rivals' tools. Bundles MCP + Skills; the hard problems (trust, perms) left unsolved on purpose.
- *MCP spec 2026-07-28* goes stateless — kills the `initialize` handshake + session-id, so any request can hit any instance; adds MCP Apps, Tasks, cache hints, stronger OAuth. `mcp-use v2` rebuilt around it.
- Anthropic confirms an in-house silicon team to power Claude.

😤 *Developer Friction*
- Human-in-the-loop is a leaky safeguard: scope violations (`cat ~/.aws/credentials`) missed 35%; `npm run analyze` approved 64.7% *even when the script curls secrets out*; 7% approve everything (`--dangerously-skip-permissions` fans).
- Fake Agent Skills already slipped past security scanners this year — and the new standard hands trust decisions back to each client.

💰 *Monetization Patterns*
- *Aident Loadout*: agents → 1,000+ apps, *27,000+ executable actions + full audit history*. Selling the action catalog and audit trail, not the model.
- *headroom*: token compression as library/proxy/MCP — 60–95% fewer tokens on JSON, priced on savings.
- *Manufact/mcp-use*: MCP hosting, cross-client testing, publishing checks — picks-and-shovels for the plugin era.

🛠️ *Meta-Agents, Skills & MCP Tools*
- `bytedance/deer-flow` — long-horizon SuperAgent harness (sandboxes, memory, subagents, message gateway).
- `obra/superpowers` — agentic skills framework (the one you run).
- `crewAI`, `langflow`, `dify` — orchestration all trending; `punkpeye/awesome-mcp-servers` is the registry.
- `Remembrane` — agent memory in one SQLite file, zero deps.

💡 *Opportunity Sparks*
- *Trust broker for Agent Plugins*: an MCP-native permission/policy layer clients drop in — the spec explicitly leaves this open. This is the UIPE thesis: temporal perception catching the exfil the human misses.
- *Pre-flight scanner-as-a-service* for `plugin.json` bundles — flag the `npm-run-analyze` pattern in MCP+Skills; price per-scan or per-seat.
- *Audit-trail-as-a-service* for MCP calls, cross-client and standalone (Aident bundles it) — per-agent pricing could clear $50–100/mo.

*Action today:* Read the Agent Plugins 1.0 spec + the MCP 2026-07-28 stateless revision, then write a one-page note on where UIPE slots in as the permissions/perception layer the spec omits. That's the wedge.
