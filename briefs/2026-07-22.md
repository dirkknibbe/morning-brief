☀️ *Morning Brief* — Wednesday, Jul 22

*A third of popular MCP servers are quietly broken for agents* — and nobody's linting for it. Teng Li ran `mcpgrade` (Lighthouse-style scorecard, no API key) against 36 popular servers: 11 scored D/F, including *official* servers from MongoDB (66 errors), Notion, Airtable (66 errors), and firecrawl at the very bottom (134 errors). They're all 100% spec-compliant. The failure is upstream of the protocol: undocumented parameters. Schemas get generated from zod/OpenAPI, nobody adds `.describe()`, and the model gets `url: string` with zero hint which URL, what format, what constraints. This is the highest-leverage hour in the whole ecosystem right now — and directly your lane.

🔥 *Hot Signals*
- *mcpgrade* — `npx mcpgrade --stdio "..."` scores any MCP server on descriptions/naming/schema/token cost. Instant scorecard, leaderboard at tengli.dev.
- *Libretto Browser Tools SDK* — 6 tools (snapshot → exec Playwright → diff) give any agent a real browser at *55% lower cost* than dev-browser/agent-browser/playwright-cli across 26 tasks. Directly adjacent to UIPE's turf.
- *Jack Dorsey's Buzz* — team chat + AI agents + Git hosting in one (Block). Another bet that agents live inside the collaboration surface, not beside it.

😤 *Developer Friction*
- The MCP "undocumented-parameter epidemic": token score fine, schema fine, *descriptions score of zero*. Models call the wrong tool or hallucinate args. Documentation discipline, not catalog size, is the differentiator.
- *"I built an AI agent I can't turn off. Now it won't listen to me"* (front-page) — the control/kill-switch gap in autonomous agents is becoming a felt pain, not a thought experiment.

💰 *Monetization Patterns*
- Libretto sells on *cost delta* (55% cheaper) with a published benchmark method — the winning pitch for agent infra is $/task, not features.
- Headroom: compress tool outputs/logs/RAG before the LLM — 20% fewer tokens for coding agents, 60–95% for JSON. Ships as library, proxy, *and* MCP server — the "sell the same thing three form factors" play.

🛠️ *Meta-Agents, Skills & MCP Tools*
- *Observal* (Show HN) — local registry + analytics for your Skills, MCPs, and Agents; scope and share them. Agent-native infra keeps compounding (12 mentions/7d).
- *open-and-async/mcp* — content-marketing MCP for a book; "ships the method, never the manuscript." A distribution pattern worth noting.
- *Chalk* — Mac diagramming app with a built-in MCP for Claude.

📈 *Still Trending*
- *agent-native infra* (12 mentions/7d) — registries, observability, harnesses.
- *MCP verification* & *MCP monetization* — the grade-your-server story is the same thread, now with a tool behind it.

💡 *Opportunity Sparks*
- *Hosted MCP quality gate* — CI action + dashboard that runs mcpgrade-style checks on every deploy and blocks D/F servers. MCPAASTA-native, recurring by seat.
- *`.describe()` autofixer* — an agent that reads a server's zod/OpenAPI schema and generates parameter descriptions from usage + docs. One-time fix, but every server needs it now.
- *Kill-switch-as-a-service* — a supervisory MCP layer that gives any autonomous agent a hard stop, budget cap, and audit log. The "can't turn it off" pain is real.

*Action today:* Run `npx mcpgrade --stdio` against your own UIPE MCP server. If it scores below A on descriptions, that's your morning — and a screenshot-worthy "we grade A" trust signal for the MCPAASTA listing.
