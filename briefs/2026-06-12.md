☀️ *Morning Brief* — Friday, Jun 12

Quiet HN day, but one clear thread: *agent cost & limit observability is becoming a product category.* Two fresh tools ship the same week to fill a gap Anthropic left open — individuals on Pro/Max get no usage history, no per-session cost, no forecast. That's a friction with paying users attached.

🔥 *Hot Signals*
• *Workplane* (Show HN) — agents publish artifacts to per-URL pages; humans comment inline, agents build on them. One-prompt skill install, works across Claude/Codex/Cursor/MCP. This is MCPAASTA distribution in the wild.
• *Anthropic apologizes* for invisible Claude Fable guardrails — covert anti-distillation throttling, now being made visible. Fable will refuse more, openly.
• *Kong* shipped an Agent Gateway to govern A2A traffic + used agentic AI to auto-fix its flakiest tests.

😤 *Developer Friction*
• Pro/Max users are flying blind on usage — `/usage` shows "now" but no history or trajectory. Real anger, real spend.
• "Tailwind and Slop Apps" + a self-aware "I procrastinate by building anti-procrastination tools" — AI-slop fatigue is loud.
• Ask HN: *any local LLM without a GPU for agentic workflows?* — the local-agent on-ramp is still painful.

💰 *Monetization Patterns*
• *state-harness* — Rust core + Python SDK, split BSL 1.1 / Apache license (open core, commercial gate baked in). pip-installable, PyPI download badge as proof.
• *Claumon* — free single-binary local dashboard as top-of-funnel for Claude Code power users.
• Kong monetizes the *agent traffic layer* (gateway + usage metering/billing).

🛠️ *Meta-Agents, Skills & MCP Tools*
• *state-harness* — Lyapunov-stability monitor for multi-turn agents. Detects token/context spirals, classifies the failure pattern, projects cost — *zero extra LLM calls*. `GrowthRatioGuard(token_budget)` wraps your loop. Directly relevant to UIPE + any harness.
• Workplane's `skill.md` install pattern is a clean template for shipping an MCP-adjacent capability with a token in `~/.config`.
• Evergreen movers unchanged: obra/superpowers, deer-flow, CowAgent, nanobot, crewAI.

📈 *Still Trending*
• Meta-agent harnesses (seen 7d) — deer-flow, CowAgent, nanobot keep climbing.
• MCP infrastructure + awesome-mcp-servers — steady.
• x402 / per-call agent commerce — still simmering, no new catalyst today.

💡 *Opportunity Sparks*
• *Agent-run cost guardian as a service*: state-harness logic, but hosted — agents POST step tokens, you return trip/no-trip + a spiral report. Bill per-call (x402) or flat $5/mo per agent. The friction is proven; the SDK is single-purpose.
• *UIPE angle*: pair temporal UI perception with a "did the agent spiral / loop on this UI?" detector — Lyapunov-style stall detection for *browser* agents specifically, where retries are silent and expensive.
• *Skill-install distribution*: copy Workplane's one-prompt `skill.md` + token flow as the install UX for a UIPE MCP — lowest-friction on-ramp I've seen.

🎯 *Today's action:* `pip install state-harness`, wrap a 10-turn UIPE agent loop in `GrowthRatioGuard`, and see if its spiral classifier fires on a real browser-retry loop. If it does, that's your validation for a hosted browser-agent stall detector.
