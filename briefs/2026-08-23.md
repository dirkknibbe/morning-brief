☀️ *Morning Brief* — Sunday, Aug 23

*MCP tool-definition bloat just became a paid product category.* `onemcp.dev` launched on HN today selling exactly one thing: your 20 MCP servers flood every request with ~15k tokens of schemas *before the model does any work*. Their fix — one portal, 3 meta-tools (search/describe/execute), ~97% smaller footprint. This is validation that context overhead is a real, chargeable pain — and it's squarely your beat.

🔥 *Hot Signals*
- `onemcp.dev` — one endpoint for N servers, native OAuth + "Code Mode" so agents script tools instead of loading hundreds of defs.
- `Ryan-AI-Studios/hands` — Rust MCP that drives *real* Chrome via OS SendInput (no CDP, no Playwright). `observe` = screenshot + UIA element list + optional DOM ids. This is UIPE-adjacent — a competitor and a validation.
- `meetless.ai` (MLA) — governance layer that decides which captured agent decision still has *authority*. Sits between Claude Code/Codex and the codebase.

😤 *Developer Friction*
- "The model spends its window on tool schemas, not your problem." Every MCP server taxes every request. This is the money quote of the day.
- Context rot, reframed: "your agent can find the right answer and still act on the wrong one." Bigger windows make stale-decision conflict *worse*, not better.
- Hands' author admits confirm-before-money is "best-effort classification," and screenshot/DOM text is untrusted input the model may still obey. Injection risk is unsolved for screen-driving agents.

💰 *Monetization Patterns*
- `onemcp` sells token reduction per-portal — charging for savings, not features.
- `topagent.lol` — pure pay-to-rank agent leaderboard ("outbid to hit #1"). Satirical, but a live micropayment rail with real bids.
- `meetless` — free-during-beta, governance is the paid layer.

🛠️ *Meta-Agents, Skills & MCP Tools*
- `bytedance/deer-flow` — long-horizon SuperAgent harness (sandboxes, memory, subagents, message gateway). Still anchoring the harness beat.
- `headroomlabs-ai/headroom` — token compression as library/proxy/MCP (60–95% on JSON). Same thesis as onemcp, different layer.
- `obra/superpowers` (#1) + new `techskills` — skill-module ecosystem keeps growing.

📈 *Still Trending*
- MCP portals/registries (theme x4) — consolidation is the story.
- harness-as-a-service (x4), agent-memory/authority (x3) — both accelerating.

💡 *Opportunity Sparks*
1. *Perception diff for UIPE:* onemcp compresses schemas; you compress *state*. Return only "what changed since last observe" so agents don't re-perceive a full DOM each step. Sell the diff, not the snapshot.
2. *Injection fence MCP:* Hands admits DOM/screen text is untrusted but the model isn't told. A drop-in that tags/quarantines untrusted screen text — safety add-on for any screen-driving agent.
3. *Metered MCP gateway:* onemcp validates the portal shape. Add x402-style pay-per-meta-tool-call = MCPAASTA in one hop.

*Action today:* Prototype a `perceive_diff` meta-tool on UIPE — returns only UI/DOM changes since the agent's last observe. Dogfood against one site, measure token savings vs a full snapshot. That number is the pitch.
