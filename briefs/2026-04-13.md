☀️ *Morning Brief* — Sunday, Apr 13

Claude's quality crisis just got a receipt: AMD's AI director analyzed 6,852 Claude Code sessions and found thinking depth dropped 67%, code reads before edits fell from 6.6 to 2.0, and the model started editing files it hadn't read. Anthropic confirmed they silently switched default effort from "high" to "medium" and added "adaptive thinking" that sometimes allocates *zero* thinking tokens. AMD has already switched providers. This is the clearest signal yet that API-dependent workflows need circuit breakers.

🔥 *Hot Signals*
- *Cache TTL regression exposed* — Claude's prompt cache TTL silently dropped from 1h to 5m around March. Any 5-minute pause forces a full cache rewrite at 12.5x the read rate. Boris (Anthropic) says subagents use 5m, main agent still 1h — but users disagree. One dev already shipped a keepalive workaround (yesmem).
- *Moat* — new tool runs AI agents in isolated containers where credentials flow through the network, not env vars. Agents act without seeing secrets. Guides for Claude Code, Codex, Gemini.
- *llama.cpp gets native audio* — Gemma-4 E2A/E4A models bring STT directly into llama-server. Early reports: better than Whisper for Spanish, but VRAM-hungry and unstable on longer audio.
- *Speculative decoding* for Gemma 4 31B with E2B draft model: +29% avg speed, +50% on code generation.

😤 *Developer Friction*
- *"The golden age is over"* — 919 upvotes, 221 comments. Users report all major LLMs degrading simultaneously. The emerging consensus: use Sonnet instead of Opus (burns fewer tokens, avoids throttling), or force deep reasoning via custom instructions.
- *Effort workaround going viral* — paste "Always reason thoroughly" into Claude Chat custom instructions. Users report "Claude is actually thinking again." The fact that paying customers need a prompt hack to get full capability is the real story.
- *Trust gap* — HN thread on trusting agents with API keys (23 comments). Redactify (macOS/iOS) redacts sensitive data before sending to LLMs. The agent trust tooling market is wide open.

💰 *Monetization Patterns*
- yesmem ships a keepalive ping feature to avoid cache rewrite costs — solves a real billing pain for Claude Code users
- Moat positions as the security layer for agent deployment — charges for the container runtime, not the AI
- Claudraband (104 HN points) packages Claude Code power-user workflows — skills frameworks becoming a product category

🛠️ *New MCP/Agent Tools*
- *prompt-refinery* — MCP server that refines prompts using retrieval evidence (open source)
- *Revdiff* — TUI diff reviewer with inline annotations designed for AI agent workflows
- *deer-flow* (ByteDance) — open-source long-horizon SuperAgent with sandboxes, memory, subagents
- *Nb* — notebook CLI designed for both humans and AI agents

📈 *Still Trending*
- *browser-use* (4th day) — browser automation for agents keeps climbing
- *awesome-mcp-servers* (3rd day) — the directory everyone checks
- *n8n* (4th day) — workflow automation with AI capabilities
- *context7* (4th day) — up-to-date docs for LLM code editors

💡 *Opportunity Sparks*
1. *Cache health monitor MCP* — an MCP server that tracks cache TTL state, pings to keep context alive, and alerts when you're about to eat a 12.5x rewrite. yesmem proves demand; an MCP-native version integrates directly into Claude Code sessions.
2. *Agent credential vault* — Moat's "credentials through the network" pattern as a standalone MCP service. Agents request scoped, time-limited tokens; you see an audit log. Pairs perfectly with UIPE's web UI perception.
3. *Effort canary* — lightweight tool that monitors thinking-token allocation per turn and flags when the model goes lazy (zero thinking tokens = hallucination risk, per AMD's data). Could be a CLI wrapper or MCP hook.

*Today's action:* Look at yesmem's keepalive implementation and estimate whether a cache-health MCP server is a weekend build. The cache TTL pain is acute, people are already paying for workarounds, and MCP distribution gives you install-and-go.
